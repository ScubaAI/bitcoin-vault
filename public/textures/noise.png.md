# Noise Texture

Placeholder for procedural noise texture.

This texture is used for:
- Particle effects in the 3D vault
- Background noise overlays
- Material roughness maps

In production, this can be generated programmatically using the `createNoiseTexture()` function in `src/lib/three/utils.ts`
